import { Theme } from "@mui/material";

// ----------------------------------------------------------------------

const Drawer = (_theme: Theme) => {
  return {
    MuiDrawer: {
      styleOverrides: {},
    },
  };
};

export default Drawer;
